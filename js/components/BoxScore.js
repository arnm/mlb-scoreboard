/**
 * @jsx React.DOM
 */

var React = require('react');

var BoxScore = React.createClass({

  render: function() {
    return (
      <div>BoxScore</div>
    );
  }

});

module.exports = BoxScore;
